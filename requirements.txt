# ─────────────────────────────────────────────────────────────────────────────
# WipeTrace — Python Dependencies
# Python >= 3.10 required (uses match/case syntax and union type hints)
# Python 3.12 recommended
#
# Install:
#   pip install -r requirements.txt
#
# Optional GPU/acceleration extras:
#   pip install -r requirements.txt --extra-index-url https://pypi.org/simple
# ─────────────────────────────────────────────────────────────────────────────


# ── Web framework & server ────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0          # includes websockets, httptools, uvloop
aiofiles>=23.2.1                   # async file I/O for upload streaming
python-multipart>=0.0.9            # multipart/form-data (file uploads)


# ── ML stack ─────────────────────────────────────────────────────────────────
numpy>=1.26.0                      # vectorised block feature extraction
scikit-learn>=1.4.0                # RandomForest / ExtraTrees / GradientBoosting / IsolationForest
joblib>=1.3.2                      # model persistence (.pkl) + parallel feature extraction


# ── Standard library (no install needed — listed for documentation) ───────────
# asyncio          — async scan executor
# bz2, zlib        — compression simulation in ML training data
# collections      — Counter for byte frequency analysis
# concurrent.futures — ProcessPoolExecutor (parallel block classification)
# dataclasses      — BlockResult, Region, ScanStats, PartitionEntry
# hashlib          — SHA-256 evidence hashing + custody chain
# json             — analysis result serialisation
# math             — entropy calculation helpers
# multiprocessing  — worker count detection
# os, platform, socket, sys — custody chain system metadata
# pathlib          — cross-platform file paths
# struct           — MBR/GPT binary parsing
# time, datetime   — scan timing + custody timestamps
# uuid             — session ID generation
